{
	"compilerOptions": {
		// node.js 를 평범하게 사용하고 다양한 걸 import 하거나 export 하게 만드는 것
		"module": "CommonJS",
		"target": "ES2015", // typeScript => ES2015 버전 javascript 로 바꿔라
		"sourceMap": true, // Sourcemap 처리를 하고 싶은지?
		"outDir": "dist"
	},
	"include": ["src/**/*"], // 어떤 파일들이 컴파일 과정에 포함되는지 알려주는 과정 || 컴파일 과정에서 포함할 파일의 배열을 적으면 된다.
	"exclude": ["node_modules"]
}
